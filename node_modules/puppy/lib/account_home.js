(function() {
  var fs, path, spawn;
  path = require("path");
  fs = require("fs");
  spawn = require("child_process").spawn;
  module.exports.command = function(argv) {
    var ssh;
    return (ssh = spawn("ssh", ["-T"]));
  };
}).call(this);
