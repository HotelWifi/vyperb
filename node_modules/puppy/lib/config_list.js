(function() {
  var fs;
  fs = require("fs");
  module.exports.command = function(argv) {
    var _i, _len, _ref, _result, configuration, property;
    try {
      configuration = JSON.parse(fs.readFileSync("" + (process.env["HOME"]) + "/.puppy/configuration.json", "utf8"));
    } catch (e) {
      configuration = {};
    }
    _result = []; _ref = "email server".split(/\s+/);
    for (_i = 0, _len = _ref.length; _i < _len; _i++) {
      property = _ref[_i];
      _result.push(configuration[property] ? process.stdout.write("" + (property) + "=" + (configuration[property]) + "\n") : null);
    }
    return _result;
  };
}).call(this);
